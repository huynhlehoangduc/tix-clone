import { YoutubePopupDirective } from './youtube-popup.directive';

describe('YoutubePopupDirective', () => {
  it('should create an instance', () => {
    const directive = new YoutubePopupDirective();
    expect(directive).toBeTruthy();
  });
});
